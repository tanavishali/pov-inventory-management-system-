import { OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { Model } from 'mongoose';
import { EventEmitter } from 'events';
import { UserDocument } from '../users/schemas/user.schema';
import { ShopDocument } from '../shops/schemas/shop.schema';
import { OrderDocument } from '../orders/schemas/order.schema';
import { UdharDocument } from '../udhar/schemas/udhar.schema';
import { ProductDocument } from '../products/schemas/product.schema';
export declare class WhatsappService extends EventEmitter implements OnModuleInit, OnModuleDestroy {
    private userModel;
    private shopModel;
    private orderModel;
    private udharModel;
    private productModel;
    private activeSockets;
    private connectionStatuses;
    private connectedJids;
    private pendingConnects;
    private welcomeSentShops;
    private reconnectTimers;
    constructor(userModel: Model<UserDocument>, shopModel: Model<ShopDocument>, orderModel: Model<OrderDocument>, udharModel: Model<UdharDocument>, productModel: Model<ProductDocument>);
    onModuleInit(): Promise<void>;
    onModuleDestroy(): Promise<void>;
    getConnectionInfo(shopId: string): {
        status: "disconnected" | "connecting" | "connected";
        phone: string | null;
    };
    updateSettings(shopId: string, whatsappAdminNumbers: string[]): Promise<UserDocument>;
    getSettings(shopId: string): Promise<string[]>;
    connect(shopId: string): Promise<void>;
    disconnect(shopId: string): Promise<void>;
    private getMessageText;
    private handleIncomingMessage;
    private isAuthorized;
    private getHelpText;
    private getShopsReport;
    private getBillsReport;
    private getOrdersReport;
    private getUdharReport;
    private getStockReport;
    private getTodayReport;
    private getUsersReport;
    private getAdminInfoReport;
}
